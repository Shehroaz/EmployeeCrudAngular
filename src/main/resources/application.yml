server:
  port: 1001
spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:file:C:\employeeCrud\db
    username: king
    password: king
    driver-class-name: 'org.h2.Driver'
  jpa:
    database-platform: 'org.hibernate.dialect.H2Dialect'
    hibernate:
      ddl-auto: update
    show-sql: true